# Source-qualified combat context campaign

Base: verified post-PR197 main `3ce9b150a7a5b1f4bc8947f74411d6df9ac859bf`. Candidate: draft [PR198](https://github.com/bellebing/Bellibing-simulator/pull/198), `codex/source-qualified-combat-context-2026-09-15`. No merge authorized.

## Product baseline and first ranking

57 released Characters; 54 have 492 isolated direct-hit facts and exact Echo-card comparison. 47 presets contain two ENGINE_MODELED rotations and 45 source sequences. Only Augusta/Ciaccona are DPS_READY. Queue: 83 open edges / 72 IDs, partition 24/24/7/11/17; readiness 43/3/9/2. All six Reference Team blockers remain open.

Generate the deterministic inventory with `node --experimental-strip-types scripts/report-combat-context-coverage.mjs --output <report.json>`. It joins all 54 hit Characters to existing facts, presets, equipment, team identities, primitive support and exact execution dependencies. It copies no canonical gameplay amounts. A missing effect row is not proof of absence. S6 source presets are explicitly outside the existing S0 hit contract; their equipment recommendations do not authorize S6 execution.

| Rank | Existing source family | Character / preset discovery reach | Reason |
| --- | --- | --- | --- |
| 1 | Character base/Minor Fortes and compatible weapon core | 54 Characters | Remove repeated stat arithmetic for every current hit path; disputed Abyss Surges remains excluded. |
| 2 | Permanent weapon effects | 39 / 41 | Rank values and unconditional self contracts already exist. |
| 3 | Static Sonata effects | 42 / 44 | Existing equipped-set facts; actual membership must still be explicit. |
| 4 | Main-slot static Echo effects | 21 / 21 | Exact species/wielder contracts; specialized pending branches remain separate. |
| 5 | Existing weapon cast/damage/heal effects | 20 / 20 | Reuse actual event primitives without creating triggers or uptime. |

These counts measure discovery, not complete context assembly. Baseline capability ladder by Character identity: L0 60, L1 54, L2 54, L3–L7 two each. Partial static assembly must be reported separately from a complete L3 context. Existing engine availability is not new dependency closure.

## Contract and parked work

The new consumer must independently rebuild current/candidate stats, preserve the exact hit/component/count/element/equipment identity, and retain every unassembled contribution as a named caller obligation. Missing context produces PENDING without damage. Static source values cannot establish conditional uptime. Build-dependent effects require fresh per-build proof. A hit delta is not a whole-build upgrade or rotation DPS.

Parked without new evidence: S03/S10 lifecycle; Heron conflict; Fallacy variant; Defier's Thorn timing; Mourning Aix semantics; Denia/Moth scaling; Voidwing hit occurrence; Abyss Surges core-stat conflict. No Reference Team constant or resource/denominator assumption is changed. UI PR196, BUG-030 and root Flamewing WIP remain separate.

## Checkpoints

- Discovery: first inventory reproduces 54/492, 47 presets and all 83/72 open dependencies. Status reconciliation is published on the candidate branch. Runtime assembly and its gates are still in progress.
- Base/core slice: `assembleCharacterHitContext` reads level-90 Character/scaling, max Minor Fortes and compatible level-90 weapon stats; `compareCharacterHitWithAssembledContext` supplies both independently assembled builds to the existing PR197 comparison. All 54 current hit Characters are exercised. Each unassembled context scope requires fresh explicit per-build evidence; missing state returns PENDING without damage. Abyss Surges remains source-blocked regardless of passive coverage.
- Permanent weapon slice: 60 canonical permanent SELF stat effects have identity-only discovery. The assembler applies supported selected-weapon R1–R5 values once; all conditional branches stay in the requirement manifest. Tests exercise every available compatible weapon/rank, source drift, stale evidence, omitted values and existing Ciaccona/Augusta paths. Partial contributions advance all 54 hit consumers but do not claim 54 complete L3 contexts. Full checkpoint follows; static Sonata/main-Echo families remain the next safe candidates.
- First full checkpoint at `5ac1a558ed3992f21f7795a616006013a512dead`: 872/872 tests, all nine audits, strict build and whitespace PASS. [Verify1154](https://github.com/bellebing/Bellibing-simulator/actions/runs/34927216720) and [Export1053](https://github.com/bellebing/Bellibing-simulator/actions/runs/34927216666) SUCCESS. Logs retain full tests and real Chrome evidence. Re-ranking keeps static Sonata and main-Echo ahead of conditional timeline work. No exact profile dependency closes: static stat arithmetic does not establish event/resource/recipient/denominator proof.
- Static Sonata slice: 30 source-defined permanent stat effects compose from an explicit five-slot species/set assignment; current/candidate retain those identities. The supported subset requires distinct species and validates canonical membership/COST, without claiming duplicate-species legality or counting semantics. Activated source-conflicted/specialized and conditional branches remain named requirements. Coordinated Attack DMG stays outside the ordinary hit taxonomy. Missing equipment remains PENDING; no recommended set is silently equipped.
- Static main-Echo slice: 50 exact VERIFIED_MODELED main-slot stat facts compose from explicit equipped species/main slot. Existing wielder filtering is reused; conditional identity-restricted facts and ALREADY_MODELED_UPSTREAM rows remain unassembled under the clarified contract. Cast effects, specialized state and omitted effect coverage remain explicit requirements. Every supported fact is exercised, and the actual Ciaccona preset's weapon/Gusts/Kelpie configuration reproduces the existing owned-build static arithmetic using source-table regression rolls. These rolls are not a claim about user-owned gear. Main-Echo completion is followed by the second full checkpoint and further ranking, not a freeze.
- Second full checkpoint at `a1fdfe900e913c61d027115dbf7ef8b51da4b41e`: 878/878 tests, all nine audits, strict build, whitespace and three real-Chrome regressions PASS. [Verify1156](https://github.com/bellebing/Bellibing-simulator/actions/runs/34978202340) and [Export1055](https://github.com/bellebing/Bellibing-simulator/actions/runs/34978202364) SUCCESS; actual logs read. No profile edge or readiness changed.
- Weapon cast context: 33 existing timed SELF stat bindings now feed the same hit comparison after the existing primitive validates each cast. Eleven current Character/preset weapon cohorts are reached: Augusta, Calcharo, Carlotta, Chixia, Iuno, Jinhsi, Lingyang, Lumi, Lupa, Rebecca and Roccia. Each build must supply its own exact card key, weapon/rank, actor, event kind/time, source qualification, isolated prior state, no-later-activation evidence and same-timestamp query ordering. Missing events remain PENDING_EVENT. Known inactive windows contribute zero only with that explicit lifecycle evidence. Canonical amounts/durations are reused; unsupported amplification and specialized state remain unassembled. Tests exercise all bindings and real preset weapon cohorts at R1–R5; synthetic events never establish profile timing.
- Healing context and event classification: the existing two reviewed weapon healing windows plus Rejuvenating Glow's existing applied-heal primitive can resolve their exact named hit-context requirements only from per-build HEAL_APPLIED proof, correct healer/team/equipment, isolated activation and explicit query ordering. Missing proof is classified PENDING_EVENT, never a zero. The healing fixture uses a legal 4/3/3/1/1 loadout; no production/gameplay value changed in that repair. Exact checkpoint `37c552de15dfa5562a13fe17c5935db7aed8185d` passed Verify1164 and Export1063.
- Bounded Sonata damage context: the reviewed four-window Sonata damage primitive is not treated as four generic Character stats. Only `S22_3PC_HEAVY_CR` and `S29_5PC_AERO` cross the ordinary Character direct-hit boundary: the former contributes CRIT Rate only when the selected canonical direct hit is HEAVY, while the latter remains the canonical Aero DMG Bonus. `S22_3PC_ECHO_CR` and `S29_5PC_ECHO_CR` remain outside this bridge because Echo Skill CRIT Rate belongs to an Echo-hit consumer, not ordinary Character CRIT Rate. Both accepted contracts require exact set count, owner, source-qualified damage event, isolated lifecycle and explicit same-timestamp ordering independently per build. No value or duration is copied.
- Fourth full checkpoint at `7ba8837c7375e8b46bc7661dbc89133d5cbc43bc`: 900/900 tests, all deterministic source/profile/readiness gates, strict build, real-Chrome regressions and whitespace PASS. [Verify1170](https://github.com/bellebing/Bellibing-simulator/actions/runs/35309178723) and [Export1069](https://github.com/bellebing/Bellibing-simulator/actions/runs/35309178726) SUCCESS on the exact head. Export artifact inspection confirms PARTIAL_L4 = 17, adding Galbrena and Sigrika as Sonata-damage context cohorts. No pending edge, profile execution dependency, complete L3–L7 count or DPS_READY disposition changed.
- Target-qualified Sonata context: all three reviewed target-window stat effects now feed the isolated Character-hit assembler only after the existing primitive proves the exact selected set/piece count, owner, source-qualified attack/hit event, same-target pre-trigger observation and condition state. `S11_5PC_SPECTRO` requires the exact ten-stack Spectro Frazzle condition (including the already-reviewed Zani Eternal Radiance equivalence path); `S17_5PC_CR` and `S17_5PC_AERO` require explicit Aero Erosion on the attacked target. Each build supplies independent event/equipment evidence plus isolated activation/no-later-activation/query ordering. A target state is never inferred from the equipped set, team or timestamp.
- Fifth full checkpoint at `feb6f0924fcca4e5d987b8a7abe517ad7c131ffe`: 905/905 tests, all deterministic gates, strict build, real-Chrome regressions and whitespace PASS. [Verify1177](https://github.com/bellebing/Bellibing-simulator/actions/runs/35309732718) and [Export1076](https://github.com/bellebing/Bellibing-simulator/actions/runs/35309732761) SUCCESS. Exact Export artifact `10532523117` reports PARTIAL_L4 = 21. New target cohorts are Cartethyia, Jiyan, Rover (Aero) and Zani; no pending profile edge or DPS readiness changes.
- Cross-owner Sonata incoming transfers: the two reviewed direct Outro transfers (`S08_5PC_INCOMING_ATK`, `S12_5PC_INCOMING_HAVOC`) now compose only when each build supplies a canonical released source owner, explicit source 5-piece equipment proof, the actual Outro -> selected incoming Character event, isolated prior/no-later activation evidence and hit-time ordering. Duplicate same-effect sources are rejected because cross-source stacking is unreviewed. The incoming Character's own preset never proves source teammate gear, so this capability intentionally creates no derived preset reach and leaves `selected-team-effects` pending.
- Sixth full runtime checkpoint at `1b32e30ddf39dbe964b545934a38d1d83df67bea`: **912/912 tests, zero failures**, all deterministic gates, strict build, real-Chrome regressions and whitespace PASS. [Verify1189](https://github.com/bellebing/Bellibing-simulator/actions/runs/35310604719) and [Export1088](https://github.com/bellebing/Bellibing-simulator/actions/runs/35310604666) SUCCESS. Exact Export artifact `10533077820` contains both incoming-transfer capabilities; independent artifact reconstruction confirms PARTIAL_L4 remains **21**, all six Reference Team blockers remain, and readiness is unchanged. Capability is not profile execution or dependency closure.
- Cross-owner Echo incoming transfers: the three reviewed ordinary-stat contracts now compose through the same isolated-hit boundary only from explicit source main-Echo/equipment proof plus their exact arm/Outro/recipient requirements. Voidwing Moth retains its reviewed Rank-5/use-to-Outro and prior-state requirements; Reminiscence: Denia requires its summon-to-Outro window; Hyvatia additionally requires the actual incoming Intro. Tied Echo-arm/Outro timestamps require explicit Echo-before-Outro proof. Duplicate same-effect activations remain rejected because stacking is unreviewed. Canonical source labels are preserved in event evidence while the existing stat projection normalizes Fusion/All Attribute DMG Bonus for damage arithmetic.
- Seventh full runtime checkpoint at `16adbb0d937cc335844866d9346aa9d411f830cb`: **918/918 tests, zero failures**, all deterministic gates, strict build, real-Chrome regressions and whitespace PASS. [Verify1196](https://github.com/bellebing/Bellibing-simulator/actions/runs/35312106001) and [Export1095](https://github.com/bellebing/Bellibing-simulator/actions/runs/35312106003) SUCCESS. Exact Export artifact `10533294801` contains all three Echo incoming-transfer support rows. Independent artifact reconstruction confirms PARTIAL_L4 remains **21**, readiness remains 43/3/9/2 and six Reference Team blockers remain open.
- Cross-owner Static Mist transfer: the already-reviewed `STM-NEXT-ATK` ordinary ATK% transfer now composes only when each build proves a canonical released source Character, compatible verified `static-mist`, explicit R1-R5 equipment evidence, actual source Outro -> selected incoming Character identity and isolated lifecycle/query ordering. Rank values remain owned by the canonical weapon effect row. Duplicate same-effect source activations are rejected because stacking is unreviewed. The incoming Character preset/team identity never proves source weapon equipment or uptime.
- Eighth full runtime checkpoint at `13fdee310a5d699778a97d3d8a55c1926b464a3d`: **924/924 tests, zero failures**, all deterministic gates, strict build, real-Chrome regressions and whitespace PASS. [Verify1205](https://github.com/bellebing/Bellibing-simulator/actions/runs/35312595859) and [Export1104](https://github.com/bellebing/Bellibing-simulator/actions/runs/35312595835) SUCCESS. Exact Export artifact `10534455518` contains exactly one weapon incoming-transfer support row (`STM-NEXT-ATK`). Independent artifact reconstruction confirms PARTIAL_L4 remains **21**, readiness remains 43/3/9/2 and six Reference Team blockers remain open.
- Cross-owner Rejuvenating Glow team context: `REJUV_ATK` now composes only from explicit source owner, exact 5-piece Sonata-7 equipment evidence, selected canonical team membership and a source-qualified `HEAL_APPLIED / VERIFIED_HEAL_ALLY` event. The healed target may be a different selected ally; the selected hit Character must still be an explicit team member. Self-owned and cross-owner Rejuvenating Glow cannot double count without reviewed stacking semantics. No heal, uptime or source teammate gear is inferred.
- Cross-owner Stellar Symphony team context: `SSY-TEAM-ATK` now composes only from the exact Shorekeeper source identity, verified Stellar Symphony R1-R5 equipment, the canonical Chaos Theory healing Skill fact, selected canonical team membership and isolated lifecycle/query ordering. Rank values remain in the canonical weapon effect row. The selected Shorekeeper own-equipment path cannot be bypassed through the team bridge.
- Ninth full runtime checkpoint at `515b375de3f4ff5c4d3bd11f3649df6c85a70a79`: **936/936 tests, zero failures**, all deterministic gates, strict build, real-Chrome regressions and whitespace PASS. [Verify1222](https://github.com/bellebing/Bellibing-simulator/actions/runs/35313733105) and [Export1121](https://github.com/bellebing/Bellibing-simulator/actions/runs/35313733094) SUCCESS. Exact Export artifact `10534262863` contains one `sonataTeamHealHitContext` row and one `weaponTeamStatHitContext` row. These cross-owner/teamgear capabilities are intentionally excluded from preset-derived PARTIAL_L4 reach, which remains **21**; readiness, all 83 edges / 72 IDs and all six Reference Team blockers remain unchanged.
- Bounded Fallacy team-stat context: the existing `fallacy-support-windows-v1` split is now locked to canonical `FALLACY_TEAM_ATK = ATK%` and `FALLACY_WIELDER_ER = Energy Regen`. Only the TEAM ATK% window crosses into ordinary Character-hit context. Each build must prove a canonical released source owner, exact Fallacy main-slot equipment, selected canonical team membership, the source-qualified `ECHO_SKILL_CAST`, isolated lifecycle and query ordering. The wielder-only ER window remains outside the team bridge, and a selected Character cannot use the cross-owner path to bypass their own equipment path.
- Tenth full runtime checkpoint at `720baf97790614a3d24b9b62dbb9971d5b933391`: **942/942 tests, zero failures**, all deterministic gates, strict build, real-Chrome regressions and whitespace PASS. [Verify1230](https://github.com/bellebing/Bellibing-simulator/actions/runs/35317271836) and [Export1129](https://github.com/bellebing/Bellibing-simulator/actions/runs/35317271784) SUCCESS. Exact Export artifact `10536032527` contains exactly one `echoTeamStatHitContext` row for `FALLACY_TEAM_ATK`. Independent artifact reconstruction confirms PARTIAL_L4 remains **21**. The Reference Team dependency `shorekeeper-fallacy-team-atk-augusta-window-overlap` remains open because the generic capability supplies no real profile timestamp/overlap proof.
- Scoped amplification composition: the existing direct-hit kernel remains unchanged with one scalar `amplification` slot. A new typed boundary classifies only exact reviewed Character-hit labels into all-damage, element or Character damage-class scope. It resolves zero or one active applicable term; two or more active applicable terms return `PENDING_STACKING` rather than being added or multiplied. `Echo Skill DMG Amplification` remains outside Character direct-hit scope. When one source-qualified term is active, residual caller amplification must be exactly zero, preventing silent cross-source double counting.
- Shorekeeper Binary Butterfly now feeds that boundary only from exact Shorekeeper Outro, selected canonical team membership, isolated lifecycle and per-build query ordering. Bloodpact's Pledge `BPP-TEAM-AERO` now feeds the same boundary only from canonical Rover (Aero), verified Bloodpact R1–R5 equipment, exact Unbound Flow cast and explicit nearby/on-field recipient eligibility. Its Aero scope is retained separately from the adapter's source scope. A simultaneous Shorekeeper all-DMG + Bloodpact Aero hit is regression-locked to `PENDING_STACKING`.
- Eleventh full runtime checkpoint at `db5186ffe66994df2c0424de3fd6ef1603e67563`: **960/960 tests, zero failures**, all deterministic gates, strict build, real-Chrome regressions and whitespace PASS. [Verify1246](https://github.com/bellebing/Bellibing-simulator/actions/runs/35329672131) and [Export1145](https://github.com/bellebing/Bellibing-simulator/actions/runs/35329672132) SUCCESS. Exact Export artifact `10540796176` contains both `shorekeeperTeamAmplification` and `weaponTeamAmplificationHitContext` support surfaces; canonical BPP source data remains unchanged. These capabilities do not alter preset-derived PARTIAL_L4, readiness, pending edges or Reference Team blockers.
- Character Outro scoped amplification: the existing canonical Outro transfer adapter now feeds the typed single-active boundary for **12 Character facts / 19 Character-hit-compatible terms**. Actual incoming recipient, complete explicit recipient switch-out history, per-build query ordering and source-specific isolated-activation policy are mandatory. Qiuyuan's Echo Skill-only amplification remains outside Character direct-hit scope. Dual-term source Outros remain distinct; a hit matching exactly one term can resolve, while a hit matching two active terms is `PENDING_STACKING`. Canonical values/durations remain owned by the source facts.
- Iuno's separate reviewed Outro adapter now feeds the same boundary as Heavy Attack DMG Amplification only. It requires exact Iuno -> incoming recipient identity, explicit switch-out history, conservative `NONE_ACTIVE` proof, no-later-activation proof and same-timestamp ordering. Generic availability does not establish Reference Team/Jiyan timing or overlap and does not close `iuno-wan-light-augusta-event-overlap`.
- Twelfth full runtime checkpoint at `22d2a748978cccec64345187671ba0785af0c7b6`: **975/975 tests, zero failures**, all deterministic gates, strict build, real-Chrome regressions and whitespace PASS. [Verify1261](https://github.com/bellebing/Bellibing-simulator/actions/runs/35331016298) and [Export1160](https://github.com/bellebing/Bellibing-simulator/actions/runs/35331016116) SUCCESS. Exact Export artifact `10541161001` contains one `iunoOutroAmplification` support row plus **19 `characterOutroAmplification` rows across 12 facts**; Qiuyuan is absent from that Character-hit export. PARTIAL_L4/readiness/pending-edge counts remain unchanged.
- Lux & Umbra scoped weapon amplification: `LU-HEAVY-AMP` now reuses the existing qualified weapon damage-event proof instead of duplicating an activation path. Exact selected Lux & Umbra R1-R5, source-qualified ECHO damage, isolated lifecycle and query ordering are required independently per build. Only Character HEAVY hits match the typed amplification scope; `LU-ECHO-AMP` remains Echo-hit-only. Any overlap with another applicable amplification term remains `PENDING_STACKING`.
- Lethean Elegy defense arithmetic: `LE-DEF` now reuses the same reviewed ECHO-damage window that already owns Lethean's conditional state, but is kept out of ordinary stat/amplification composition. An active DEF Ignore term requires explicit enemy DEF plus proof that no other defense modifiers are hidden in the residual snapshot. The caller's baseline `defenseMultiplier` must exactly match the existing no-modifier defense kernel before canonical R1-R5 DEF Ignore is applied. Multiple active DEF Ignore terms remain unsupported stacking, and source-blocked/ambiguous DEF-ignore families are not promoted.
- Thirteenth full runtime checkpoint at `720ba472341b2f67ed3aa94e52e15289f73a1783`: **987/987 tests, zero failures**, all deterministic gates, strict build, real-Chrome regressions and whitespace PASS. [Verify1274](https://github.com/bellebing/Bellibing-simulator/actions/runs/35334479067) and [Export1173](https://github.com/bellebing/Bellibing-simulator/actions/runs/35334479060) SUCCESS. Exact Export artifact `10542099832` contains exactly one `weaponDamageAmplificationHitContext` row (`LU-HEAVY-AMP`) and one `weaponDamageDefenseHitContext` row (`LE-DEF`). Independent artifact reconstruction confirms PARTIAL_L3 = **54**, PARTIAL_L4 = **21**, 492 direct-hit facts and two DPS_READY Characters. No profile edge, readiness or Reference Team blocker closes.
- Solsworn Ciphers scoped defense context: `SCIP-AERO-DEF` now reuses the existing exact ECHO-damage window but can affect only explicit Aero Character hits. Exact selected Solsworn Ciphers R1-R5, isolated lifecycle/query ordering, explicit enemy DEF and proof that no other defense modifiers are hidden in the residual snapshot are mandatory per build. Non-Aero hits observe the source window as known inactive for the selected hit rather than inheriting the DEF Ignore.
- Lux & Umbra overlap defense context: `LU-DEF` is now derived only while both already-reviewed Lux windows (`LU-HEAVY-AMP` and `LU-ECHO-AMP`) are simultaneously active for the exact selected wielder. No independent timer or activation event is invented. The Character-hit bridge keeps `LU-ECHO-AMP` state-only, consumes both exact window proofs, and applies the canonical rank value only through the existing single-active DEF-ignore boundary with explicit enemy DEF/no-hidden-modifier proof.
- Fourteenth full runtime checkpoint at `ac7fd5ec4941c6f72393da3fcc2906981f35aa77`: **1000/1000 tests, zero failures**, all deterministic gates, strict build, real-Chrome regressions and whitespace PASS. [Verify1286](https://github.com/bellebing/Bellibing-simulator/actions/runs/35335698019) and [Export1185](https://github.com/bellebing/Bellibing-simulator/actions/runs/35335698014) SUCCESS. Exact Export artifact `10542786560` is bound to this head. PARTIAL_L3/PARTIAL_L4 and readiness/pending-edge counts remain unchanged; no Reference Team blocker closes.
- Woodland Aria target resistance context: `WA-AERO-RES` now has a dedicated reviewed target-window contract and feeds a separate typed resistance boundary rather than DEF or ordinary stat arithmetic. Activation requires exact selected Woodland Aria R1-R5, a source-qualified `HIT_TARGET` on the same explicit selected target, pre-trigger Aero Erosion observation for that exact target/time, isolated lifecycle/query ordering, explicit target base resistance and proof that no other resistance modifiers are hidden in the residual multiplier. Only explicit Aero hits can consume the reduction; same-hit trigger ordering and expiration remain caller-proven.
- Fifteenth full runtime checkpoint at `ace8d701580f12d99167e8eadb3db4c275b3c4c4`: **1007/1007 tests, zero failures**, all deterministic gates, strict build, real-Chrome regressions and whitespace PASS. [Verify1296](https://github.com/bellebing/Bellibing-simulator/actions/runs/35337534126) and [Export1195](https://github.com/bellebing/Bellibing-simulator/actions/runs/35337534233) SUCCESS. Exact Export artifact `10544105330` contains exactly one `weaponTargetResistanceHitContext` row for `WA-AERO-RES`. Independent artifact reconstruction shows Ciaccona's Woodland Aria preset adds one real event-context cohort: PARTIAL_L3 remains **54**, PARTIAL_L4 increases **21 → 22**, direct-hit facts remain 492 and only Augusta/Ciaccona remain DPS_READY. All 83 edges / 72 IDs and all six Reference Team blockers remain open.
- Woodland Aria self Aero window: `WA-AERO` now uses a new timestamped caller-qualified `AERO_EROSION_APPLIED` event instead of translating the historical event-indexed rotation adapter. Exact Woodland Aria R1-R5, wielder, trigger target, non-empty source fact, positive applied stacks, verified application qualification, isolated lifecycle and same-timestamp query ordering are required. Canonical rank value and 10-second duration remain owned by the weapon row. The self window persists independently of the later hit target and contributes only through the existing Aero-DMG stat projection; no Character free-text note is parsed to infer application occurrence.
- Cooldown-qualified cast family: `CS-ATK`, `EC-ATK`, `FA-ATK`, `RJ-ATK` and `WR-ATK` now share one exact contract for the canonical 16-second ATK% window triggered by Resonance Skill with a 20-second source cooldown. Each activation requires exact weapon/rank/actor/cast plus caller-proven `cooldownReadyAtSeconds`, explicit cooldown-readiness qualification, `NONE_ACTIVE` prior state and no later activation through the selected hit. The paired flat resource effects remain separate; proving the ATK window never fabricates Resonance/Concerto Energy.
- Sixteenth full runtime checkpoint at `cd62fe9c22b2ef9e81296d69bb69102b25f4dd31`: **1020/1020 tests, zero failures**, all deterministic gates, strict build, real-Chrome regressions and whitespace PASS. [Verify1311](https://github.com/bellebing/Bellibing-simulator/actions/runs/35346779894) and [Export1210](https://github.com/bellebing/Bellibing-simulator/actions/runs/35346779876) SUCCESS. Exact Export artifact `10547895585` contains exactly one `WA-AERO` status-application row, five cooldown-cast rows and the existing `WA-AERO-RES` row. Independent artifact reconstruction confirms PARTIAL_L3 = **54**, PARTIAL_L4 = **22**, 492 direct-hit facts, two DPS_READY Characters and Reference Team still not ready. `WA-AERO` reaches the same Ciaccona cohort as `WA-AERO-RES`; the cooldown batch reaches already-counted Augusta, so no cohort is double-counted.
- Freeze Frame explicit Glacio Chafe context: `FF-GLACIO` and `FF-TEAM-ATK` reuse one caller-qualified timestamped `GLACIO_CHAFE_APPLIED` occurrence but remain separate canonical windows. Exact Freeze Frame R1-R5, wielder, trigger target, non-empty source fact, positive applied stacks, source qualification, isolated lifecycle and query ordering are mandatory. The TEAM path additionally requires explicit selected canonical team membership. Cross-owner composition exposes only `FF-TEAM-ATK`; it never exports the wielder-only Glacio DMG window. Own plus teammate same-name team ATK is rejected because stacking is explicitly unreviewed.
- Seventeenth full runtime checkpoint at `2a4ec99fc00614482059349c41f32e34a1ec140b`: **1032/1032 tests, zero failures**, all deterministic gates, strict build, real-Chrome regressions and whitespace PASS. [Verify1322](https://github.com/bellebing/Bellibing-simulator/actions/runs/35364082427) and [Export1221](https://github.com/bellebing/Bellibing-simulator/actions/runs/35364082371) SUCCESS. Exact Export artifact `10555795453` contains exactly two `weaponFreezeFrameHitContext` rows. Independent artifact reconstruction confirms PARTIAL_L3 = **54**, PARTIAL_L4 increases **22 → 23**, and **Lucilla is the only newly counted preset cohort**. Cross-owner `FF-TEAM-ATK` creates no inferred preset reach. Direct-hit facts remain 492, only Augusta/Ciaccona are DPS_READY, and all 83 edges / 72 IDs plus all six Reference Team blockers remain open.

## Current partial coverage and next ranking

The deterministic report lists all 54 Characters' supported static families/subsets, missing self application, unresolved source facts and exact-build completeness. It deliberately cannot establish that only conditional context remains. PARTIAL_L3 is 54; PARTIAL_L4 reaches **twenty-three** existing Character cohorts with preset equipment recommendations supported by at least one composition bridge. Ciaccona entered through Woodland Aria `WA-AERO-RES`; the later `WA-AERO` self window reaches that same preset. Freeze Frame adds **Lucilla** as the only new checkpoint17 cohort through her exact reviewed `freeze-frame` recommendation; cross-owner `FF-TEAM-ATK` is capability-only and does not create inferred teammate reach. The five cooldown-qualified cast effects remain broad reusable capability but current preset reach adds only already-counted Augusta via Waning Redshift. Complete L3–L7 counts remain two, scoped to the existing Augusta/Ciaccona engines. Per-build contribution status distinguishes STATIC_ASSEMBLED from EVENT_QUALIFIED_ASSEMBLED; remaining obligations distinguish pending event, timeline and source/application work. These statuses are not profile readiness.

The static SELF audit found no safe high-fanout structured stat family beyond existing intrinsic data. Canonical passives currently carry prose summaries; several apparently passive facts actually require form/resource/trigger state. Jinhsi's Radiant Surge, Camellya's Seedbed and Cantarella's Healing Bonus need a canonical typed projection and explicit applicability review before composition; parsing their prose at runtime or adding a parallel numeric table is excluded. Their source facts are not re-sourced or promoted here.

Sonata cast context now composes six existing SELF bindings, reaching Carlotta, Changli, Chixia, Encore, Jinhsi and Lupa preset equipment. It reuses the existing exact trigger validator and requires the correct active piece threshold from the already validated five-card species/set assignment. Equipment/source drift, wrong owner/cast, unknown ordering, duplicate activation and another build's evidence fail closed. Missing events never grant uptime. The comparison retains species/set configuration while replacing one stat card. S10's isolated cast window does not settle Carlotta's separate lifecycle blocker.

Weapon damage context now composes three exact stat bindings (LE-SKILL, DBS-SPECTRO, UV-BASIC-BASIC) via the existing damage-event primitive. Fresh derived reach is **two** current Character/preset cohorts, Phrolova and Luuk Herssen. The broader seven-effect family also reaches Galbrena, but its amplification path is outside this stat subset. Actual source-qualified damage, exact weapon/rank/actor, per-build card evidence, isolated activation and explicit query ordering are required. Casts cannot substitute for damage. Echo-specific amplification, Heavy amplification composition and DEF-ignore remain unassembled; no arithmetic stacking rule is guessed.

After checkpoint17, Freeze Frame is bounded and Lucilla is counted once. The next candidate is Azure Oath's paired `AO-HEAVY-AMP` / `AO-DEF` Havoc-Bane-triggered window for Yangyang: Xuanling, but only if the existing canonical source review justifies a caller-qualified timestamped Havoc Bane application occurrence without parsing Character prose or treating RAW_ONLY stack mechanics as executable. The weapon rows remain the sole numeric truth; amplification must reuse the existing single-active typed scope and DEF Ignore must reuse the explicit enemy-DEF/no-hidden-modifier boundary. If exact application occurrence cannot be justified from current reviewed sources, Azure Oath stays source-semantics pending and the lane must be reranked rather than guessed. Forged Dwarf Star remains a secondary candidate; Spectral Trigger/Hack, Thunderflare/Moongazer and other stack-history families remain parked. All 83 edges / 72 IDs and all six Reference Team blockers remain open.
